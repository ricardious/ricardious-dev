---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className = "" } = Astro.props;
---

<div
  class:list={[
    "text-text-standard font-body text-[clamp(1.75rem,5vw,3.75rem)] leading-none",
    className,
  ]}
>
  {text}
</div>
